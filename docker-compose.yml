services:
  logviewer:
    build: .
    container_name: logviewer
    ports:
      - "5000:5000"
    volumes:
      - ./app/users.json:/app/users.json
      - /var/log/servidor-syslog:/logs:ro
    environment:
      - TZ=America/Bogota
      - LOG_DIR=/logs
    restart: unless-stopped
